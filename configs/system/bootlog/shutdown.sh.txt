#!/bin/zsh

files=(/tmp/log/*.log(N))

date=`date +"%Y-%m-%d-%H%M%S"`

zsh /var/system/scripts/logging/logShutdown.sh

# Shutdown Loggen
echo -e "====================================\nShutdown: $(/usr/bin/date)" >> /var/log/system/boot.log